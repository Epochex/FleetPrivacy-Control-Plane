apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: fleetprivacy-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: fleetprivacy-api
  minReplicas: {{ .Values.replicas }}
  maxReplicas: 12
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 65
